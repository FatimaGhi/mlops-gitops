apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: model-serving
spec:
  selector:
    app: model-serving
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}